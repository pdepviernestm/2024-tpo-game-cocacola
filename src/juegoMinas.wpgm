import escenario.*
import wollok.game.*
import jugador.*

program juegoMinas {
    game.title("Buscaminas")
    game.cellSize(172)
    game.height(27)
    game.width(27)
    game.addVisual(fondo)
    escenario.mostrarInstrucciones()
        
    game.start()
}
